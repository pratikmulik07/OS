#include <stdio.h>
#include <stdlib.h>

void sort(int arr[], int n){
    for(int i=0;i<n-1;i++)
        for(int j=0;j<n-i-1;j++)
            if(arr[j]>arr[j+1]){
                int t=arr[j]; arr[j]=arr[j+1]; arr[j+1]=t;
            }
}

int main() {
    int n, head, size, req[100], total = 0;

    printf("Enter number of requests: ");
    scanf("%d", &n);

    printf("Enter request queue: ");
    for(int i = 0; i < n; i++) scanf("%d", &req[i]);

    printf("Enter initial head position: ");
    scanf("%d", &head);

    printf("Enter disk size: ");
    scanf("%d", &size);

    req[n++] = head;
    sort(req, n);

    int index;
    for(int i=0;i<n;i++) if(req[i]==head) index=i;

    printf("\nHead Movement Table:\n");
    printf("Current -> Next | Movement\n");

    for(int i=index;i<n-1;i++){
        int move=abs(req[i]-req[i+1]);
        printf("%d -> %d | %d\n",req[i],req[i+1],move);
        total+=move;
    }

    printf("%d -> 0 | %d\n",req[n-1],abs(req[n-1]-0));
    total+=abs(req[n-1]);

    for(int i=0;i<index-1;i++){
        int move=abs(req[i]-req[i+1]);
        printf("%d -> %d | %d\n",req[i],req[i+1],move);
        total+=move;
    }

    printf("\nTotal Seek Length = %d\n", total);
    printf("Average Seek Length = %.2f\n", (float)total/(n-1));

    return 0;
}
